'use strict';
var fs = require('fs');
var MisproTransformer = require('../index').MisproTransformer;

var timing_file = fs.createReadStream('./assets/utterance_0.out');

var transformer = MisproTransformer();
timing_file.pipe(transformer);

transformer.on('data', function(data) {
	console.log(data);
});

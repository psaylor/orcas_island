'use strict';
var fs = require('fs');
var TimingTransformer = require('../index').TimingTransformer;

var timing_file = fs.createReadStream('./assets/utterance_0.txt');

var transformer = TimingTransformer();
timing_file.pipe(transformer);

transformer.on('data', function(data) {
	console.log(data);
});
